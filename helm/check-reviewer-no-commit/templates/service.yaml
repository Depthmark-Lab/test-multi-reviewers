apiVersion: v1
kind: Service
metadata:
  name: {{ include "check-reviewer-no-commit.fullname" . }}
  labels:
    {{- include "check-reviewer-no-commit.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "check-reviewer-no-commit.selectorLabels" . | nindent 4 }}
